namespace IContact {
  export interface IState {
    html: string
    whatsapp: string
  }
  export interface IWa {
    name: string
    location: string
    message: string
  }
}
export default IContact
